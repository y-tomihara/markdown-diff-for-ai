# AI Agent Workflow Rules

## 1. 独立した検証（Independent Review）の徹底
ソースコード（`.ts`、`.js`、`.json` など）を変更した後は、**あなた自身（Orchestrator）だけでテスト結果を判断してタスクを終了してはいけません**。
必ず `invoke_subagent` ツールを用いて `Independent Reviewer` という役割のサブエージェントを起動し、客観的な検証を行わせてください。

### サブエージェントへの指示内容
サブエージェントには以下のタスクを依頼してください：
- 対象となるUnitの `npm run test <関連するテストファイル>` を実行すること
- プロジェクト全体の `npm run compile` が成功するか確認すること
- 上記の客観的証拠に基づき、`docs/reviews/review_<Unit名>_<連番>.md` を作成し、PASS/FAIL を判定すること

## 2. ビルドとテストの自動化
コード修正に伴い、VS Code拡張機能のコンパイル済みファイル（`dist/` などの出力）を最新に保つため、ソース変更後は必ず `npm run compile` を実行してからユーザーに報告してください。

## 3. UI/UXにおけるエッジケースのテスト化
改行コード（CRLF / LF）やBOM（Byte Order Mark）など、見た目には分かりにくいがUIのDiffエンジンに影響を与える事象を発見した場合は、単に実装を修正するだけでなく、**必ずコアロジックの単体テスト（Unit Test）としてテストケースを追加**し、恒久的に防げる仕組みを作ってください。

## 4. 開発環境の競合防止（グローバル拡張機能の確認）
ローカルで拡張機能の開発・テストを行う際、動作がソースコードと一致しないなどの不可解な不具合が発生した場合は、**必ずユーザーのVS Codeにマーケットプレイス版（リリース版）の同名拡張機能がグローバルインストールされていないか確認**してください。
（コマンド例: `ls ~/.vscode/extensions/` やVS Codeの拡張機能タブの確認をユーザーに促すなど）
開発環境で正常に動作を検証するため、開発期間中はグローバルにインストールされた当該拡張機能をアンインストール、または無効化することを徹底してください。
